# Deployment validation

This file triggers the pull-request validation workflow for the first GitHub Pages release.

Validation includes synthetic catalog generation, Mc unit tests, JSON export, Python compilation, and static-site file checks.
